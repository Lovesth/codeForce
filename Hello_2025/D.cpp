#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, q;
    cin >> t;
    while (t--)
    {
        cin >> n >> q;
        vector<int> nums(n + 1);
        for (int i = 1; i <= n; i++)
        {
            cin >> nums[i];
        }
        
    }
}